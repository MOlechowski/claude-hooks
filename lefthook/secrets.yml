pre-commit:
  commands:
    gitleaks:
      run: gitleaks protect --staged --verbose
      fail_text: "secrets detected in staged files"

pre-push:
  commands:
    gitleaks-full:
      run: gitleaks detect --verbose
      fail_text: "secrets detected in repository"
