version: '3'

tasks:
  lint:
    desc: Run all linters (CI)
    cmds:
      - task lint

  test:
    desc: Run all tests (CI)
    cmds:
      - task test

  commit-lint:
    desc: Check commit messages follow conventional commits
    cmds:
      - cog check --from-latest-tag || cog check
